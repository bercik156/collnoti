= simple_form_for(@notifi) do |f|
  = f.error_notification
  .form-inputs
    = f.select :category_id, Category.all.map {|d| [d,d.id] }
    = f.input :description, class: "form-control"
    = f.input :email
  .form-actions
    = f.button :submit, "Send"
